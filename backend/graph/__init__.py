"""
JSON-native process graph package.

Provides ProcessGraph (model), store (session-scoped CRUD + cache), workspace manifest,
layout, BPMN export, validation, and step-name resolution. Used by routers and agent tools.
"""
